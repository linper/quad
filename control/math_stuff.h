/**
 * @file math_stuff.h
 * @brief Misc math stuff
 * @author Linas Perkauskas
 * @date 2023-02-04
 */

#ifndef MATH_STUFF_H
#define MATH_STUFF_H

#define MIN(a, b) ((a) <= (b) ? (a) : (b))
#define MAX(a, b) ((a) >= (b) ? (a) : (b))

#endif // MATH_STUFF_H
